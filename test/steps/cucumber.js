var English = require('yadda').localisation.English;

module.exports = English.library()
  .given("I wrote a feature in test/features", () => {

  })
  .when("I wrote the step in test/steps", () => {

  })
  .when("I run npm test", () => {

  })
  .then("the result is displayed on the console", () => {

  });
